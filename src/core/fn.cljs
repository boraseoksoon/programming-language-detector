(ns core.fn)

(defn add [x y]
  (+ x y))

(defn subtract [x y]
	(- x y))

; (ns core.fn
; 	(:require ["code-trimmer" :as trimmer])
; 	(:require ["moment" :as moment]))

; (defn add [x y]
;   (+ x y))

; (defn subtract [x y]
; 	(- x y))

; (defn indent [src] 
; 	((. trimmer -trim) src))
